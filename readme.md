entities are basically interfaces

The use of ?, !, ??
user = user??Anonymous

prisma
repository: interact with data from the source

generic class look at them, preferrably in Java

order:
models
entities
repository
controllers
routes
